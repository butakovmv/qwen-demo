server:
  port: 8080

spring:
  application:
    name: otus-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  info:
    git:
      mode: full
    build:
      enabled: true
